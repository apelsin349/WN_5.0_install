# Правила разработки WorkerNet Portal

## 🔥 КРИТИЧЕСКИЕ ТРЕБОВАНИЯ

1. **Документация**: ВСЯ документация размещается в папке `инструкции/`
   **СНАЧАЛА проверить и актуализировать документацию, ПОТОМ делать код**
2. **Реальные данные**: Разработка СРАЗУ под реальные данные (НЕ моки, НЕ заглушки)
3. **Темы**: ВСЕ компоненты ОБЯЗАТЕЛЬНО интегрированы с системой тем
4. **Локализация**: ВСЕ тексты через i18next
5. **Русский язык**: ВСЕ пользовательские тексты на русском

## Взаимодействие с пользователем
- **НЕ писать инструкции по исправлениям** - сразу делать изменения
- **Выполнять задачи, параллельно объяснять** - пользователю нужен результат, а не инструкции
- **НЕ спрашивать разрешения** на очевидные действия
- Если нужно что-то исправить/добавить - просто сделать это

## Язык проекта
- Для русскоязычных пользователей
- Все UI тексты на русском языке
- Комментарии в коде на русском
- Документация на русском

## Документация
- **ВСЯ документация** размещается в папке `инструкции/`
- Документы на русском языке
- Структурированные markdown файлы
- Актуальные инструкции и гайды

### 📊 Визуальные диаграммы (ОБЯЗАТЕЛЬНО)

**ВСЕГДА добавлять Mermaid диаграммы** при документировании:
- Архитектурных решений
- Последовательности действий (sequence diagrams)
- Логики работы (flowcharts)
- Взаимодействия компонентов (component diagrams)
- Временных последовательностей (timelines)

#### ✅ ПРАВИЛЬНО - с диаграммами:

```markdown
## Как работает функция

### Диаграмма последовательности

\`\`\`mermaid
sequenceDiagram
    participant User as 👤 Пользователь
    participant API as 🖥️ Backend
    participant DB as 💾 БД

    User->>API: Запрос данных
    API->>DB: SELECT * FROM table
    DB->>API: Результат
    API->>User: Ответ
\`\`\`

### Блок-схема

\`\`\`mermaid
flowchart TD
    Start[Начало] --> Check{Проверка}
    Check -->|OK| Success[✅ Успех]
    Check -->|Error| Fail[❌ Ошибка]
\`\`\`
```

#### ❌ НЕПРАВИЛЬНО - без диаграмм:

```markdown
## Как работает функция

1. Пользователь отправляет запрос
2. API обрабатывает
3. Возвращает результат
```

**Типы диаграмм:**
1. `sequenceDiagram` - API взаимодействия, процессы
2. `flowchart TD` - логика, алгоритмы
3. `graph TB` - архитектура, компоненты
4. `timeline` - временные последовательности
5. `stateDiagram-v2` - состояния объектов
6. `erDiagram` - структура БД

**Просмотр диаграмм:**
- В Cursor: установить "Markdown Preview Mermaid Support" → Ctrl+Shift+V
- На GitHub: диаграммы рендерятся автоматически
- Онлайн: https://mermaid.live/

📖 **Подробнее:** `инструкции/КАК_СМОТРЕТЬ_ДИАГРАММЫ.md`

**ЗАПРЕЩЕНО:**
- ❌ Описывать сложную логику без диаграмм
- ❌ Создавать PNG/JPG диаграммы вместо Mermaid
- ❌ Игнорировать визуализацию для архитектуры

### 📋 ОБЯЗАТЕЛЬНЫЙ ПРОЦЕСС РАБОТЫ С ДОКУМЕНТАЦИЕЙ:

1. **ПЕРЕД любой доработкой/исправлением/новым функционалом:**
   - ✅ Проверить наличие документации по теме в `инструкции/`
   - ✅ Если документация существует → СНАЧАЛА актуализировать её
   - ✅ Если документации нет → создать новую , кроме исправлений.(если функционал требует документирования)
   - ✅ ТОЛЬКО ПОСЛЕ актуализации документации → приступать к коду

2. **Что проверять:**
   - Архитектурные документы (АРХИТЕКТУРА_*.md)
   - Инструкции по функционалу
   - API документацию
   - Руководства пользователя
   - README файлы

3. **Что актуализировать:**
   - Описание изменений в функционале
   - Обновление примеров кода
   - Изменение API endpoints
   - Новые конфигурации
   - **Обновление/создание Mermaid диаграмм** (ОБЯЗАТЕЛЬНО для архитектурных изменений)

**ЗАПРЕЩЕНО:**
- ❌ Делать код без проверки документации
- ❌ Оставлять устаревшую документацию
- ❌ Создавать функционал без документирования
- ❌ Документировать сложную логику без Mermaid диаграмм

## Система тем
- **ОБЯЗАТЕЛЬНАЯ интеграция** всех новых компонентов с системой тем
- Использование `useTheme()` и `useThemeUtils()` из `@theme/useTheme`
- Применение констант из `@theme/constants`
- Следование цветовой палитре и типографике
- Поддержка светлой и темной тем
- Использование `sx` prop вместо `style`
- НЕ хардкодить цвета, размеры, тени

## Локализация

### 🌍 КРИТИЧЕСКИЕ ПРАВИЛА ЛОКАЛИЗАЦИИ

**ПРИ ЛЮБЫХ ИЗМЕНЕНИЯХ В ЛОКАЛИЗАЦИИ:**

#### 1. **Поддерживаемые языки**
- **Русский (ru)** - основной язык проекта
- **Английский (en)** - дополнительный язык
- Все ключи ОБЯЗАТЕЛЬНО добавлять в ОБА файла: `ru.json` и `en.json`

#### 2. **Правильное расположение ключей в JSON**
```json
// ✅ ПРАВИЛЬНО - структурированная иерархия
{
  "common": {
    "actions": {
      "save": "Сохранить",
      "cancel": "Отмена"
    }
  },
  "admin": {
    "slaManagement": "Управление SLA",
    "users": {
      "create": "Создать пользователя",
      "edit": "Редактировать пользователя"
    }
  }
}
```

**🚨 КРИТИЧЕСКИЕ ПРАВИЛА РАСПОЛОЖЕНИЯ:**
- **НЕ ДОПУСКАЕТСЯ** плоская структура для сложных разделов
- **ОБЯЗАТЕЛЬНО** группировать связанные ключи в объекты
- **ОБЯЗАТЕЛЬНО** использовать логическую иерархию (section.subsection.key)
- **ОБЯЗАТЕЛЬНО** поддерживать одинаковую структуру в ru.json и en.json

#### 3. **Проверка структуры перед добавлением ключей**

**Перед добавлением нового ключа:**
1. **Определить логическую группу** (common, admin, auth, etc.)
2. **Проверить существующую структуру** в обоих файлах локализации
3. **Добавить ключ в правильное место** согласно иерархии
4. **ОБЯЗАТЕЛЬНО добавить в ОБА языка** (ru.json и en.json)

#### 4. **Формат использования в коде**
```typescript
// ✅ ПРАВИЛЬНО - использование ключей
const { t } = useTranslation();

// Простые ключи
t('common.save')
t('common.cancel')

// Вложенные ключи
t('admin.users.create')
t('admin.slaManagement')

// С параметрами
t('common.timeAgo', { hours: 2 })
```

#### 5. **Запрещенные практики**
```typescript
// ❌ НЕДОПУСТИМО - fallback текст
t('admin.slaManagement', 'Управление SLA') // Плохо!

// ❌ НЕДОПУСТИМО - хардкод текста
const text = 'Управление SLA'; // Плохо!

// ❌ НЕДОПУСТИМО - неправильная структура JSON
{
  "adminSlaManagement": "Управление SLA", // Плохо!
  "adminUserCreate": "Создать пользователя" // Плохо!
}
```

### 📋 **Чек-лист локализации (ОБЯЗАТЕЛЬНО):**

**Перед добавлением нового текста:**
- [ ] **Определить логическую группу** (common, admin, auth, etc.)
- [ ] **Проверить существующую структуру** в ru.json и en.json
- [ ] **Добавить ключ в правильное место** согласно иерархии
- [ ] **ОБЯЗАТЕЛЬНО добавить в ru.json** с русским текстом
- [ ] **ОБЯЗАТЕЛЬНО добавить в en.json** с английским текстом
- [ ] **Проверить одинаковую структуру** в обоих файлах
- [ ] **Использовать t('section.key')** без fallback текста
- [ ] **Протестировать отображение** на обоих языках

**При изменении существующих ключей:**
- [ ] **Обновить текст в ru.json**
- [ ] **Обновить текст в en.json**
- [ ] **Проверить все места использования** ключа
- [ ] **Протестировать отображение** на обоих языках

### 🔧 **Правильные паттерны локализации**

#### 1. **Структура файлов локализации**
```
frontend/src/i18n/locales/
├── ru.json          # Русский (основной)
└── en.json          # Английский (дополнительный)
```

#### 2. **Пример правильной структуры**
```json
// ru.json
{
  "common": {
    "actions": {
      "save": "Сохранить",
      "cancel": "Отмена",
      "delete": "Удалить"
    },
    "status": {
      "active": "Активный",
      "inactive": "Неактивный"
    }
  },
  "admin": {
    "slaManagement": "Управление SLA",
    "users": {
      "create": "Создать пользователя",
      "edit": "Редактировать пользователя",
      "delete": "Удалить пользователя"
    }
  }
}

// en.json (ОБЯЗАТЕЛЬНО такая же структура!)
{
  "common": {
    "actions": {
      "save": "Save",
      "cancel": "Cancel", 
      "delete": "Delete"
    },
    "status": {
      "active": "Active",
      "inactive": "Inactive"
    }
  },
  "admin": {
    "slaManagement": "SLA Management",
    "users": {
      "create": "Create User",
      "edit": "Edit User",
      "delete": "Delete User"
    }
  }
}
```

#### 3. **Использование в компонентах**
```typescript
import { useTranslation } from 'react-i18next';

const MyComponent = () => {
  const { t } = useTranslation();

  return (
    <Box>
      <Typography variant="h4">
        {t('admin.slaManagement')}
      </Typography>
      
      <Button onClick={handleSave}>
        {t('common.actions.save')}
      </Button>
      
      <Button onClick={handleCancel}>
        {t('common.actions.cancel')}
      </Button>
    </Box>
  );
};
```

### 🚨 **КРИТИЧЕСКИЕ ПРОВЕРКИ ЛОКАЛИЗАЦИИ**

#### Перед коммитом:
```bash
# 1. Проверить, что ключи добавлены в оба файла
grep -q "admin.slaManagement" frontend/src/i18n/locales/ru.json
grep -q "admin.slaManagement" frontend/src/i18n/locales/en.json

# 2. Проверить одинаковую структуру JSON
jq 'keys' frontend/src/i18n/locales/ru.json
jq 'keys' frontend/src/i18n/locales/en.json

# 3. Проверить отсутствие fallback текста в коде
grep -r "t('.*',.*)" frontend/src/ --exclude-dir=node_modules

# 4. Проверить отсутствие хардкода текста
grep -r '"[А-Яа-я]' frontend/src/ --exclude-dir=node_modules --exclude="*.json"
```

#### После изменений локализации:
```bash
# 1. Проверить валидность JSON
jq empty frontend/src/i18n/locales/ru.json
jq empty frontend/src/i18n/locales/en.json

# 2. Проверить одинаковую структуру
diff <(jq -S 'keys' frontend/src/i18n/locales/ru.json) <(jq -S 'keys' frontend/src/i18n/locales/en.json)

# 3. Протестировать переключение языков в браузере
# Перейти на страницу и переключить язык
```

### 📊 **Диаграмма процесса локализации**

```mermaid
flowchart TD
    Start[🚀 Новый текст] --> CheckLang{Какой язык?}
    
    CheckLang -->|Русский| CheckStruct[📋 Проверить структуру ru.json]
    CheckLang -->|Английский| CheckStruct
    CheckLang -->|Оба| CheckBoth[📋 Проверить оба файла]
    
    CheckStruct --> FindGroup[🔍 Найти логическую группу]
    CheckBoth --> FindGroup
    
    FindGroup --> CheckExists{Ключ существует?}
    CheckExists -->|Да| UpdateKey[✏️ Обновить существующий]
    CheckExists -->|Нет| CreateKey[➕ Создать новый]
    
    UpdateKey --> AddToBoth[🌍 Добавить в оба языка]
    CreateKey --> AddToBoth
    
    AddToBoth --> ValidateJSON[✅ Проверить валидность JSON]
    ValidateJSON --> TestDisplay[🧪 Протестировать отображение]
    TestDisplay --> Complete[✅ Готово]
```

### 🚫 **ЗАПРЕЩЕНО В ЛОКАЛИЗАЦИИ:**

- ❌ **Добавлять ключи только в один файл** (ru.json или en.json)
- ❌ **Использовать плоскую структуру** для сложных разделов
- ❌ **Нарушать логическую иерархию** ключей
- ❌ **Использовать fallback текст** в t() функции
- ❌ **Хардкодить текст** в компонентах
- ❌ **Создавать разные структуры** в ru.json и en.json
- ❌ **Игнорировать валидность JSON** файлов
- ❌ **Добавлять ключи без проверки** существующей структуры

## Backend (Python/FastAPI)
- Docstrings на русском языке
- Комментарии на русском
- Сообщения об ошибках на русском
- Формат: `raise HTTPException(status_code=403, detail="Доступ запрещен")`

## Frontend (React/TypeScript)
- Функциональные компоненты с хуками
- TypeScript interfaces для типов
- Material-UI компоненты
- Framer Motion для анимаций
- Recharts для графиков
- **Интеграция с темой** через `useTheme()` и `useThemeUtils()`

## Структура кода
```
frontend/src/
├── pages/Admin/          # Админские страницы
├── components/           # Переиспользуемые компоненты
├── i18n/locales/         # Файлы локализации
├── api/                  # API клиент
├── theme/                # Система тем
│   ├── theme.ts          # Основные темы
│   ├── constants.ts      # Константы
│   ├── useTheme.ts       # Хук для работы с темой
│   └── README.md         # Документация темы
└── routes/               # Маршрутизация

backend-react/app/
├── routers.py            # API endpoints
├── models.py             # Модели БД
├── schemas.py            # Pydantic схемы
└── audit.py              # Аудит логирование

инструкции/               # ВСЯ документация проекта
├── ПРАВИЛА_РАЗРАБОТКИ.md # Основные правила
├── README.md             # Основная документация
└── ...                   # Остальные инструкции
```

## Работа с данными
- **ОБЯЗАТЕЛЬНО**: Разработка сразу под реальные данные
- НЕ использовать mock данные в production коде
- НЕ использовать заглушки вместо реальных API вызовов
- Тестовые данные только в тестах (в папках `tests/` и `__tests__/`)
- Валидация данных на уровне БД, API и Frontend
- Обработка граничных случаев (пустые списки, null значения, большие объемы)
- Проверка работы с реальными данными перед деплоем

**ЗАПРЕЩЕНО:**
- ❌ Хардкодить тестовые данные в production коде
- ❌ Использовать моки вместо реальных API
- ❌ Разрабатывать под "идеальные" данные
- ❌ Игнорировать edge cases

## Соглашения по коду
- Русские комментарии для сложной логики
- Docstrings для всех публичных функций
- Типизация везде где возможно
- Обработка ошибок с русскими сообщениями
- **Интеграция с темой** для всех UI элементов

## Аудит
- Логировать все CRUD операции
- Использовать `log_audit()` из `audit.py`
- Сохранять детали в JSON формате
- Указывать user_id и target_id

## Тестирование

### Общие требования
- **ОБЯЗАТЕЛЬНО**: При реализации новой функции сразу создавать тесты
- **TDD подход**: Сначала тесты, потом реализация (рекомендуется)
- **Минимальное покрытие**: 80% для новых функций
- Тестировать как позитивные, так и негативные сценарии
- Русские названия тестов приветствуются

### Backend тестирование (Python/FastAPI)

**Инструменты:**
- `pytest` - основной фреймворк
- `pytest-xdist` - **ОБЯЗАТЕЛЬНО использовать** для параллельного выполнения (2-4x быстрее)
- `pytest-timeout` - предотвращение зависания тестов
- `Faker` - генерация реалистичных тестовых данных (**с русской локализацией**)
- `Factory Boy` - фабрики для БД моделей
- `Schemathesis` - автотестирование API из OpenAPI схемы
- `Respx` - моки для httpx запросов

**Готовые Faker фикстуры (в conftest.py):**
```python
# Используйте эти фикстуры вместо хардкода данных:
def test_create_user(fake_user):
    """fake_user уже содержит реалистичные данные с русской локализацией."""
    response = client.post("/api/v1/users/", json=fake_user)
    assert response.status_code == 201

def test_create_ticket(fake_ticket):
    """fake_ticket содержит title, description, priority."""
    response = client.post("/api/v1/tickets/", json=fake_ticket)
    assert response.status_code == 201

# Доступные фикстуры:
# - fake          - базовый Faker (ru_RU)
# - fake_user     - пользователь (username, email, first_name, last_name, phone)
# - fake_ticket   - тикет (title, description, priority)
# - fake_operator - оператор (company, inn, phone, address, email)
# - fake_comment  - комментарий (content, is_internal)
```

**Запуск тестов:**
```bash
# Параллельное выполнение (ОБЯЗАТЕЛЬНО использовать!)
pytest -n auto

# С покрытием
pytest -n auto --cov=backend-react --cov-report=html

# С таймаутом
pytest --timeout=300
```

**ЗАПРЕЩЕНО:**
- ❌ Хардкодить тестовые данные - используйте Faker фикстуры!
- ❌ Запускать тесты без -n auto (медленно)
- ❌ Использовать моки вместо реальных API в интеграционных тестах

### Frontend тестирование (React/TypeScript)

**Инструменты:**
- `Jest` - unit тестирование
- `@testing-library/react` - тестирование компонентов
- `MSW (Mock Service Worker)` - **ОБЯЗАТЕЛЬНО использовать** для моков API
- `@testing-library/user-event` - **ОБЯЗАТЕЛЬНО использовать** вместо fireEvent
- `jest-axe` - **ОБЯЗАТЕЛЬНО добавлять** тесты доступности для каждого компонента
- `Playwright` - E2E тестирование

**MSW для моков API:**
```typescript
// MSW автоматически перехватывает запросы (настроено в setupTests.ts)
test('загрузка списка тикетов', async () => {
  render(<TicketList />);

  // MSW автоматически вернет моковые данные из handlers.ts
  await waitFor(() => {
    expect(screen.getByText('Проблема с авторизацией')).toBeInTheDocument();
  });
});

// Переопределение handler для конкретного теста:
import { server } from '../../setupTests';
import { http, HttpResponse } from 'msw';

test('обработка ошибки API', async () => {
  server.use(
    http.get('/api/v1/tickets/', () => {
      return HttpResponse.json({ detail: 'Server Error' }, { status: 500 });
    })
  );

  render(<TicketList />);
  await waitFor(() => {
    expect(screen.getByText(/Ошибка/)).toBeInTheDocument();
  });
});
```

**user-event для реалистичных взаимодействий:**
```typescript
import userEvent from '@testing-library/user-event';

test('создание тикета', async () => {
  const user = userEvent.setup();
  render(<TicketForm />);

  // Используйте user-event вместо fireEvent!
  await user.type(screen.getByLabelText('Заголовок'), 'Новый тикет');
  await user.click(screen.getByRole('button', { name: 'Создать' }));

  await waitFor(() => {
    expect(screen.getByText('Тикет создан')).toBeInTheDocument();
  });
});
```

**jest-axe для тестов доступности:**
```typescript
import { axe } from 'jest-axe';

test('компонент доступен для скринридеров', async () => {
  const { container } = render(<TicketForm />);

  // ОБЯЗАТЕЛЬНО добавлять для каждого нового компонента!
  const results = await axe(container);
  expect(results).toHaveNoViolations();
});
```

**Полный пример (комбинация всех инструментов):**
```typescript
test('полный флоу создания тикета', async () => {
  const user = userEvent.setup();
  const { container } = render(<TicketForm />);

  // 1. Тест доступности
  const results = await axe(container);
  expect(results).toHaveNoViolations();

  // 2. Реалистичные взаимодействия (user-event)
  await user.type(screen.getByLabelText('Заголовок'), 'Тест');
  await user.selectOptions(screen.getByLabelText('Приоритет'), 'высокий');
  await user.click(screen.getByRole('button', { name: 'Создать' }));

  // 3. MSW автоматически перехватит запрос
  await waitFor(() => {
    expect(screen.getByText('Тикет создан успешно!')).toBeInTheDocument();
  });
});
```

**ЗАПРЕЩЕНО:**
- ❌ Использовать fireEvent - используйте user-event!
- ❌ Моки API вручную - используйте MSW!
- ❌ Пропускать тесты доступности - обязательно jest-axe!
- ❌ Хардкодить API ответы в тестах - настройте MSW handlers

**Документация:**
- 📖 [README_ТЕСТИРОВАНИЕ.md](README_ТЕСТИРОВАНИЕ.md) - краткая справка
- 📋 [ШПАРГАЛКА_ТЕСТИРОВАНИЕ.md](ШПАРГАЛКА_ТЕСТИРОВАНИЕ.md) - все команды
- 📘 [инструкции/тестирование/](инструкции/тестирование/) - полная документация
- 💡 [frontend/src/__tests__/examples/ExampleTest.test.tsx](frontend/src/__tests__/examples/ExampleTest.test.tsx) - примеры кода

## Структура хранения тестов
- **ВСЕ тесты** размещаются в строго определенных папках
- **НЕ создавать** тесты в корне проекта или случайных местах
- **Следовать** единой структуре для всех типов тестов

### Backend тесты (Python/FastAPI)
```
backend-react/tests/
├── unit/                    # Unit тесты (test_models.py, test_schemas.py)
├── integration/             # Интеграционные тесты (test_api_integration.py)
├── e2e/                     # E2E тесты (test_admin_workflows.py)
├── performance/             # Тесты производительности (test_load_testing.py)
├── security/                # Тесты безопасности (test_security.py)
├── critical/                # Критические тесты (test_critical_api.py)
└── conftest.py              # Конфигурация pytest
```

### Frontend тесты (React/TypeScript)
```
frontend/src/__tests__/
├── unit/                    # Unit тесты (components/, hooks/, utils/)
├── integration/             # Интеграционные тесты (api-integration.test.tsx)
├── e2e/                     # E2E тесты (operator-onboarding.spec.ts)
├── pwa/                     # PWA тесты (manifest.test.ts, service-worker/)
├── pages/                   # Тесты страниц
└── routes/                  # Тесты маршрутизации
```

### E2E тесты (Playwright/Cypress)
```
e2e/tests/                   # E2E тесты (auth.spec.ts, dashboard.spec.ts)
```

### Правила размещения тестов:
1. **Unit тесты**: В папке `unit/` по типу компонента
2. **Интеграционные тесты**: В папке `integration/`
3. **E2E тесты**: В папке `e2e/` или `e2e/tests/`
4. **Тесты производительности**: В папке `performance/`
5. **Тесты безопасности**: В папке `security/`
6. **Критические тесты**: В папке `critical/`

### Именование файлов:
- **Backend**: `test_*.py` или `*_test.py`
- **Frontend**: `*.test.ts/tsx` или `*.spec.ts/tsx`
- **E2E**: `*.spec.ts`

### НЕ ДОПУСКАЕТСЯ:
- Создавать тесты в корне проекта (кроме conftest.py)
- Размещать тесты в случайных папках
- Смешивать типы тестов в одной папке
- Использовать неправильное именование файлов

## Git
- Коммиты на русском
- Формат: "Добавлено: управление SLA"
- Подробное описание изменений

## Примеры

### Backend endpoint:
```python
@router.get("/admin/sla-policies/")
async def list_sla_policies(...):
    """Возвращает список всех политик SLA, отсортированных по приоритету."""
    if not user.is_staff:
        raise HTTPException(status_code=403, detail="Доступ запрещен")
    # Остальной код...
```

### Frontend component с интеграцией темы:
```typescript
import { useTheme } from '@mui/material/styles';
import { useThemeUtils } from '@theme/useTheme';
import { useTranslation } from 'react-i18next';

const MyComponent = () => {
  const theme = useTheme();
  const { constants, colors } = useThemeUtils();
  const { t } = useTranslation();

  return (
    <Box sx={{
      p: theme.spacing(2),
      borderRadius: constants.borderRadius.medium,
      boxShadow: constants.shadows.light,
      backgroundColor: colors.surface,
      color: colors.onSurface,
    }}>
      <Typography variant='h4'>
        {t('admin.slaManagement')}
      </Typography>
    </Box>
  );
};
```

### Локализация:
```json
// ru.json
{
  "admin": {
    "slaManagement": "Управление SLA",
    "createSLA": "Создать политику SLA",
    "users": {
      "create": "Создать пользователя",
      "edit": "Редактировать пользователя"
    }
  },
  "common": {
    "actions": {
      "save": "Сохранить",
      "cancel": "Отмена"
    }
  }
}

// en.json (ОБЯЗАТЕЛЬНО такая же структура!)
{
  "admin": {
    "slaManagement": "SLA Management", 
    "createSLA": "Create SLA Policy",
    "users": {
      "create": "Create User",
      "edit": "Edit User"
    }
  },
  "common": {
    "actions": {
      "save": "Save",
      "cancel": "Cancel"
    }
  }
}
```

### Использование в компонентах:
```typescript
import { useTranslation } from 'react-i18next';

const MyComponent = () => {
  const { t } = useTranslation();

  return (
    <Box>
      <Typography variant="h4">
        {t('admin.slaManagement')}
      </Typography>
      
      <Button onClick={handleSave}>
        {t('common.actions.save')}
      </Button>
      
      <Button onClick={handleCancel}>
        {t('common.actions.cancel')}
      </Button>
    </Box>
  );
};
```

### Документация:
```markdown
# инструкции/НОВАЯ_ФУНКЦИЯ.md

## Описание
Подробное описание новой функции

## Использование
Примеры использования

## API
Описание API endpoints
```

### Работа с реальными данными:
```typescript
// ❌ НЕПРАВИЛЬНО - хардкод тестовых данных
const users = [
  { id: 1, name: 'Тест Пользователь' },
  { id: 2, name: 'Другой Тест' }
];

// ✅ ПРАВИЛЬНО - загрузка реальных данных
const { data: users, isLoading } = useQuery('users', fetchUsers);

// ❌ НЕПРАВИЛЬНО - мок API
const mockApiCall = () => Promise.resolve({ data: mockData });

// ✅ ПРАВИЛЬНО - реальный API вызов
const apiCall = async () => {
  const response = await fetch('/api/users');
  return response.json();
};

// ✅ ПРАВИЛЬНО - обработка edge cases
if (!users || users.length === 0) {
  return <EmptyState message={t('users.noUsers')} />;
}

// ✅ ПРАВИЛЬНО - обработка больших списков
{users.slice(0, pageSize).map(user => (
  <UserCard key={user.id} user={user} />
))}
```


### Backend тесты (с новыми инструментами):
```python
import pytest

# ✅ ПРАВИЛЬНО - используем Faker фикстуры
def test_create_user(fake_user):
    """Тест создания пользователя с реалистичными данными."""
    response = client.post("/api/v1/users/", json=fake_user)
    assert response.status_code == 201
    assert response.json()["email"] == fake_user["email"]

def test_create_ticket(fake_ticket):
    """Тест создания тикета с русским текстом."""
    response = client.post("/api/v1/tickets/", json=fake_ticket)
    assert response.status_code == 201
    assert response.json()["priority"] in ["низкий", "средний", "высокий", "критический"]

# ❌ НЕПРАВИЛЬНО - хардкод данных
def test_create_user_wrong():
    response = client.post("/api/v1/users/", json={
        "username": "test",  # Плохо: хардкод!
        "email": "test@test.com"
    })

# Запуск: pytest -n auto backend-react/tests/
```

### Frontend тесты (с новыми инструментами):
```typescript
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { axe } from 'jest-axe';
import { ThemeProvider } from '@mui/material/styles';
import { theme } from '@theme/theme';
import TicketForm from '../TicketForm';

// ✅ ПРАВИЛЬНО - полный пример с новыми инструментами
test('создание тикета - полный флоу', async () => {
  const user = userEvent.setup();
  const { container } = render(
    <ThemeProvider theme={theme}>
      <TicketForm />
    </ThemeProvider>
  );

  // 1. Тест доступности (ОБЯЗАТЕЛЬНО!)
  const results = await axe(container);
  expect(results).toHaveNoViolations();

  // 2. user-event вместо fireEvent (ОБЯЗАТЕЛЬНО!)
  await user.type(screen.getByLabelText('Заголовок'), 'Новый тикет');
  await user.type(screen.getByLabelText('Описание'), 'Описание проблемы');
  await user.selectOptions(screen.getByLabelText('Приоритет'), 'высокий');

  // 3. MSW автоматически перехватит запрос
  await user.click(screen.getByRole('button', { name: 'Создать' }));

  // 4. Проверка результата
  await waitFor(() => {
    expect(screen.getByText('Тикет создан успешно!')).toBeInTheDocument();
  });
});

// ❌ НЕПРАВИЛЬНО - старый подход
test('создание тикета - неправильно', () => {
  render(<TicketForm />);

  // Плохо: fireEvent вместо user-event
  fireEvent.change(screen.getByLabelText('Заголовок'), {
    target: { value: 'Тест' }
  });

  // Плохо: нет теста доступности
  // Плохо: нет MSW, моки API вручную
});

// Запуск: npm test
// Примеры: npm test -- ExampleTest.test.tsx
```

## Система RBAC (Role-Based Access Control)

### 🔐 КРИТИЧЕСКИЕ ПРАВИЛА RBAC

**ПРИ ЛЮБЫХ ИЗМЕНЕНИЯХ В СИСТЕМЕ КОНТРОЛЯ ДОСТУПА:**

#### 1. **Добавление новых разрешений**
```python
# ✅ ОБЯЗАТЕЛЬНО - обновить init_rbac.py
PERMISSIONS = [
    # ... существующие разрешения ...
    {
        "code": "new:permission",
        "resource": "resource_name", 
        "action": "action_name",
        "description": "Описание разрешения на русском",
    },
]

# ✅ ОБЯЗАТЕЛЬНО - назначить ролям
ROLES = {
    "Администратор": {
        "permissions": [
            # ... существующие разрешения ...
            "new:permission",  # НОВОЕ РАЗРЕШЕНИЕ
        ],
    },
}
```

**🚨 КРИТИЧЕСКОЕ ПРАВИЛО:**
- **ВСЕ новые разрешения ОБЯЗАТЕЛЬНО добавляются в роль "Администратор"**
- **Администратор должен иметь доступ ко ВСЕМ функциям системы**
- **НЕ ДОПУСКАЕТСЯ создавать разрешения без назначения роли "Администратор"**

#### 2. **Добавление новых ролей**
```python
# ✅ ОБЯЗАТЕЛЬНО - добавить в init_rbac.py
ROLES = {
    # ... существующие роли ...
    "Новая Роль": {
        "description": "Описание роли на русском",
        "permissions": [
            "permission1",
            "permission2",
            # ВСЕ необходимые разрешения
        ],
    },
}
```

#### 3. **Изменение разрешений существующих ролей**
```python
# ✅ ОБЯЗАТЕЛЬНО - обновить в init_rbac.py
ROLES = {
    "Существующая Роль": {
        "permissions": [
            # УДАЛИТЬ ненужные разрешения
            # ДОБАВИТЬ новые разрешения
            "updated:permission",
        ],
    },
}
```

### 🚨 КРИТИЧЕСКИЕ ОШИБКИ RBAC

#### ❌ НЕДОПУСТИМО - забыть обновить базу данных:
- ❌ Добавить разрешение в код, но НЕ в `init_rbac.py`
- ❌ Изменить роли в коде, но НЕ в `init_rbac.py`
- ❌ Создать новую роль, но НЕ добавить в `ROLES`
- ❌ Назначить разрешение роли, но НЕ обновить `ROLES`
- ❌ **Создать новое разрешение, но НЕ добавить в роль "Администратор"**

#### ❌ НЕДОПУСТИМО - хардкод разрешений:
```python
# ❌ КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО!
cursor.execute("""
    SELECT p.code FROM permissions p
    JOIN role_permissions rp ON p.id = rp.permission_id
    WHERE rp.role_id = 1  # Хардкод роли!
""")
```

#### ❌ НЕДОПУСТИМО - отсутствие проверки разрешений:
```python
# ❌ ОПАСНО - нет проверки разрешений
@router.get("/admin/new-feature/")
async def new_feature():
    """ОПАСНО! Нет проверки разрешений"""
    return data
```

### ✅ ПРАВИЛЬНЫЙ ПРОЦЕСС РАБОТЫ С RBAC

#### 🔄 **Обязательная последовательность действий:**

```mermaid
flowchart TD
    Start[🚀 Новая функция/изменение] --> CheckRBAC{Требует ли RBAC?}
    
    CheckRBAC -->|Да| CheckPerm{Есть ли разрешение?}
    CheckRBAC -->|Нет| Continue[Продолжить разработку]
    
    CheckPerm -->|Нет| AddPerm[➕ Добавить в init_rbac.py]
    CheckPerm -->|Да| CheckRole{Назначено ролям?}
    
    AddPerm --> AssignRole[🔗 Назначить ролям в ROLES]
    AssignRole --> UpdateBackend[🖥️ Обновить Backend]
    
    CheckRole -->|Нет| AssignRole
    CheckRole -->|Да| UpdateBackend
    
    UpdateBackend --> UpdateFrontend[💻 Обновить Frontend]
    UpdateFrontend --> TestRBAC[🧪 Тестировать RBAC]
    TestRBAC --> Deploy[🚀 Деплой]
    
    Continue --> UpdateBackend
```

#### 📋 **Чек-лист RBAC (ОБЯЗАТЕЛЬНО):**

**Перед добавлением новой функции:**
- [ ] **Определить необходимые разрешения**
- [ ] **Добавить разрешения в `init_rbac.py`**
- [ ] **ОБЯЗАТЕЛЬНО добавить новые разрешения в роль "Администратор"**
- [ ] **Назначить разрешения другим ролям в `ROLES` (если нужно)**
- [ ] **Обновить Backend API с проверкой разрешений**
- [ ] **Обновить Frontend с ProtectedRoute**
- [ ] **Протестировать доступ для разных ролей**
- [ ] **Проверить отсутствие хардкода ролей**

**При изменении существующих разрешений:**
- [ ] **Обновить `init_rbac.py`**
- [ ] **Проверить все роли, использующие разрешение**
- [ ] **Обновить Backend проверки**
- [ ] **Обновить Frontend компоненты**
- [ ] **Протестировать все затронутые функции**

**При добавлении новых ролей:**
- [ ] **Добавить роль в `ROLES`**
- [ ] **Назначить все необходимые разрешения**
- [ ] **Создать тестовых пользователей с новой ролью**
- [ ] **Протестировать доступ для новой роли**
- [ ] **Документировать назначение роли**

### 🔧 **Правильные паттерны RBAC**

#### 1. **Создание новых разрешений**
```python
# ✅ ПРАВИЛЬНО - полный процесс
# 1. Добавить в PERMISSIONS
PERMISSIONS = [
    {
        "code": "system:backup",
        "resource": "system", 
        "action": "backup",
        "description": "Управление резервными копиями системы",
    },
]

# 2. Назначить ролям
ROLES = {
    "Администратор": {
        "permissions": [
            "system:backup",  # НОВОЕ РАЗРЕШЕНИЕ
        ],
    },
}

# 3. Проверка в Backend
@router.get("/admin/backups/")
async def list_backups(current_user: User = Depends(get_current_user)):
    if not has_permission(current_user, "system:backup"):
        raise HTTPException(status_code=403, detail="Доступ запрещен")
    return backups
```

#### 2. **Проверка разрешений в Frontend**
```typescript
// ✅ ПРАВИЛЬНО - использование ProtectedRoute
<Route
  path="/admin/backups"
  element={
    <ProtectedRoute adminOnly permission="system:backup">
      <BackupManagementPage />
    </ProtectedRoute>
  }
/>

// ✅ ПРАВИЛЬНО - проверка в компонентах
const MyComponent = () => {
  const { hasPermission } = useAuth();
  
  if (!hasPermission('system:backup')) {
    return <AccessDenied />;
  }
  
  return <BackupControls />;
};
```

#### 3. **Динамическая загрузка разрешений**
```python
# ✅ ПРАВИЛЬНО - загрузка для всех ролей пользователя
def load_user_permissions(user_id: int):
    """Загружает разрешения для всех ролей пользователя"""
    
    # Получаем все роли пользователя
    user_roles = get_user_roles(user_id)
    role_ids = [role.id for role in user_roles]
    
    # Загружаем разрешения для всех ролей (НЕ хардкод!)
    role_ids_str = ','.join(map(str, role_ids))
    cursor.execute(f"""
        SELECT p.code FROM permissions p
        JOIN role_permissions rp ON p.id = rp.permission_id
        WHERE rp.role_id IN ({role_ids_str})
    """)
    
    return [row[0] for row in cursor.fetchall()]
```

### 🚨 **КРИТИЧЕСКИЕ ПРОВЕРКИ RBAC**

#### Перед коммитом:
```bash
# 1. Проверить, что все разрешения добавлены в init_rbac.py
grep -r "system:backup" backend-react/app/init_rbac.py

# 2. Проверить, что разрешения назначены ролям
grep -A 10 -B 5 "system:backup" backend-react/app/init_rbac.py

# 3. Проверить отсутствие хардкода ролей
grep -r "role_id = 1" backend-react/app/

# 4. Проверить наличие проверок разрешений в API
grep -r "has_permission" backend-react/app/

# 5. Проверить ProtectedRoute в Frontend
grep -r "ProtectedRoute.*permission" frontend/src/

# 6. Проверить, что новые разрешения добавлены в роль "Администратор"
grep -A 20 '"Администратор"' backend-react/app/init_rbac.py | grep "new:permission"
```

#### После изменений RBAC:
```bash
# 1. Перезапустить инициализацию RBAC
python3 backend-react/app/init_rbac.py

# 2. Проверить загрузку разрешений в логах
# Должно быть: DEBUG: Permission XX: system:backup for role 1

# 3. Протестировать API endpoints
curl -H "Authorization: Bearer $TOKEN" "http://localhost:8000/api/v1/auth/profile/" | jq '.permissions | contains(["system:backup"])'

# 4. Протестировать Frontend доступ
# Перейти на страницу, требующую разрешение
```

### 📊 **Диаграмма процесса RBAC**

```mermaid
sequenceDiagram
    participant Dev as 👨‍💻 Разработчик
    participant Init as 📝 init_rbac.py
    participant DB as 💾 База данных
    participant API as 🖥️ Backend API
    participant Frontend as 💻 Frontend
    
    Dev->>Init: 1. Добавить разрешение в PERMISSIONS
    Dev->>Init: 2. Назначить ролям в ROLES
    Dev->>DB: 3. Запустить init_rbac.py
    DB->>DB: 4. Создать разрешение в БД
    DB->>DB: 5. Назначить ролям в БД
    
    Dev->>API: 6. Добавить проверку разрешений
    API->>DB: 7. Загрузить разрешения пользователя
    DB->>API: 8. Вернуть список разрешений
    
    Dev->>Frontend: 9. Добавить ProtectedRoute
    Frontend->>API: 10. Проверить разрешения
    API->>Frontend: 11. Вернуть результат проверки
```

## Критические требования

1. **Документация**: ВСЕ документы в `инструкции/`
   - 📋 **СНАЧАЛА проверить и актуализировать документацию, ПОТОМ делать код**
   - 📊 **ОБЯЗАТЕЛЬНО добавлять Mermaid диаграммы** для визуализации логики
2. **Реальные данные**: Разработка СРАЗУ под реальные данные (НЕ моки, НЕ заглушки в production)
3. **Темы**: ВСЕ компоненты интегрированы с системой тем
4. **Локализация**: ВСЕ тексты через i18next
5. **Русский язык**: ВСЕ пользовательские тексты на русском
6. **Типизация**: TypeScript везде где возможно
7. **Аудит**: Логирование всех операций
8. **Тестирование**: ОБЯЗАТЕЛЬНО тесты для новых функций
9. **Покрытие**: Минимум 80% для новых функций
10. **Визуализация**: Mermaid диаграммы для архитектуры и сложной логики
11. **RBAC**: Правильная реализация системы контроля доступа

## 🚨 КРИТИЧЕСКИЕ ЗАМЕЧАНИЯ ИЗ ТЕСТИРОВАНИЯ

### ❌ НЕДОПУСТИМЫЕ ПРОБЛЕМЫ (Заблокировать деплой):

1. **Frontend тесты с ошибками >5%**
   - Текущий статус: 99/104 ошибок (95%!)
   - Требование: <5% ошибок в тестах
   - Проверка: `npm test` должен проходить успешно

2. **Критические линтер ошибки >1000**
   - Требование: <1000 замечаний
   - Проверка: `flake8`, `ruff`, `mypy` без критических ошибок

3. **Критические проблемы безопасности >100**
   - Требование: <100 проблем
   - Проверка: `bandit`, `safety` без критических уязвимостей

4. **Backend тесты не запускаются**
   - Требование: >50 тестов должны выполняться
   - Проверка: `pytest` должен находить и выполнять тесты

5. **Покрытие кода <80%**
   - Текущий статус: 16.1% backend
   - Требование: >80% для новых функций
   - Проверка: `coverage` отчет должен показывать >80%

### 🔧 ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ ПЕРЕД КОММИТОМ:

```bash
# 1. Проверка тестов (с новыми инструментами!)
npm test                              # Frontend тесты должны проходить
pytest -n auto backend-react/tests/   # Backend тесты ПАРАЛЛЕЛЬНО

# 2. Проверка линтера
flake8 backend-react/       # <1000 замечаний
ruff check backend-react/   # <100 замечаний
mypy backend-react/         # 0 ошибок типизации

# 3. Проверка безопасности
bandit -r backend-react/    # <100 проблем
safety check               # 0 критических уязвимостей

# 4. Проверка покрытия
pytest -n auto --cov=backend-react --cov-report=term  # >80% покрытие
npm test -- --coverage                                 # Frontend покрытие

# 5. Проверка RBAC (ОБЯЗАТЕЛЬНО!)
grep -r "role_id = 1" backend-react/app/               # НЕ должно быть хардкода
grep -r "system:backup" backend-react/app/init_rbac.py # Должно быть в init_rbac.py
grep -r "has_permission" backend-react/app/           # Должны быть проверки разрешений
grep -r "ProtectedRoute.*permission" frontend/src/    # Должны быть ProtectedRoute

# 6. Проверка локализации (ОБЯЗАТЕЛЬНО!)
jq empty frontend/src/i18n/locales/ru.json             # Валидность ru.json
jq empty frontend/src/i18n/locales/en.json             # Валидность en.json
diff <(jq -S 'keys' frontend/src/i18n/locales/ru.json) <(jq -S 'keys' frontend/src/i18n/locales/en.json) # Одинаковая структура
grep -r "t('.*',.*)" frontend/src/ --exclude-dir=node_modules # НЕ должно быть fallback текста
grep -r '"[А-Яа-я]' frontend/src/ --exclude-dir=node_modules --exclude="*.json" # НЕ должно быть хардкода русского текста

# 7. Pre-commit hooks (Husky автоматически запускает)
# - ESLint --fix
# - Prettier --write
# - Jest --findRelatedTests
```

### 📋 ТРЕБОВАНИЯ К КАЧЕСТВУ КОДА:

1. **Длина строки**: Максимум 120 символов (не 175!)
2. **Неиспользуемые импорты**: НЕ ДОПУСКАЮТСЯ
3. **Неопределенные переменные**: НЕ ДОПУСКАЮТСЯ
4. **Отсутствующие типы**: НЕ ДОПУСКАЮТСЯ в TypeScript
5. **Отсутствующие тесты**: НЕ ДОПУСКАЮТСЯ для новых функций

### 🚫 ЗАПРЕЩЕНО ДЕПЛОИТЬ ПРИ:

- ❌ Наличие моков/заглушек в production коде
- ❌ Хардкод тестовых данных в production коде
- ❌ Frontend тесты с ошибками >5%
- ❌ Линтер ошибки >1000
- ❌ Проблемы безопасности >100
- ❌ Backend тесты не запускаются
- ❌ Покрытие кода <80%
- ❌ Критические ошибки MyPy
- ❌ Неиспользуемые зависимости
- ❌ Дублирование кода >5%
- ❌ **Хардкод ролей в RBAC** (`role_id = 1`)
- ❌ **Отсутствие разрешений в `init_rbac.py`**
- ❌ **Отсутствие проверок разрешений в API**
- ❌ **Отсутствие ProtectedRoute в Frontend**
- ❌ **Новые разрешения НЕ добавлены в роль "Администратор"**
- ❌ **Невалидные JSON файлы локализации** (ru.json, en.json)
- ❌ **Разная структура** в ru.json и en.json
- ❌ **Fallback текст** в t() функциях
- ❌ **Хардкод русского текста** в компонентах
- ❌ **Ключи локализации только в одном языке**

### ✅ ОБЯЗАТЕЛЬНО ПЕРЕД ДЕПЛОЕМ:

- ✅ **Документация проверена и актуализирована** (в `инструкции/`)
- ✅ **Mermaid диаграммы добавлены/обновлены** для новой функциональности
- ✅ **Проверено на реальных данных** (НЕТ моков/заглушек в production коде)
- ✅ Все тесты проходят успешно
- ✅ Линтер показывает <1000 замечаний
- ✅ Безопасность: <100 проблем
- ✅ Покрытие кода >80%
- ✅ MyPy без ошибок
- ✅ **RBAC проверен**: разрешения в `init_rbac.py`, роли обновлены, проверки в API и Frontend
- ✅ **Новые разрешения добавлены в роль "Администратор"**
- ✅ **Отсутствие хардкода ролей** в коде
- ✅ **Локализация проверена**: валидные JSON файлы, одинаковая структура ru.json и en.json
- ✅ **Отсутствие fallback текста** в t() функциях
- ✅ **Отсутствие хардкода русского текста** в компонентах
- ✅ **Все ключи локализации добавлены в оба языка**
- ✅ Документация обновлена после изменений
- ✅ Changelog заполнен

### 🔄 ПРОЦЕСС РАЗРАБОТКИ И ИСПРАВЛЕНИЯ:

1. **Обнаружение проблемы/Новая задача** → Создать issue
2. **📋 Проверка документации** → Найти и актуализировать связанную документацию в `инструкции/`
3. **🔐 Проверка RBAC** → Если функция требует разрешений:
   - Определить необходимые разрешения
   - Добавить в `init_rbac.py`
   - **ОБЯЗАТЕЛЬНО добавить в роль "Администратор"**
   - Назначить другим ролям в `ROLES` (если нужно)
4. **🌍 Проверка локализации** → Если добавляется новый текст:
   - Определить логическую группу (common, admin, auth, etc.)
   - Проверить существующую структуру в ru.json и en.json
   - Добавить ключ в правильное место согласно иерархии
   - **ОБЯЗАТЕЛЬНО добавить в ОБА языка** (ru.json и en.json)
   - Проверить валидность JSON файлов
5. **Исправление/Разработка** → Следовать стандартам
6. **Тестирование** → Все проверки проходят
7. **📊 Обновление диаграмм** → Добавить/обновить Mermaid диаграммы для новой логики
8. **🔐 Проверка RBAC** → Убедиться что:
   - Разрешения добавлены в базу данных
   - **Новые разрешения добавлены в роль "Администратор"**
   - API проверяет разрешения
   - Frontend использует ProtectedRoute
   - Отсутствует хардкод ролей
9. **🌍 Проверка локализации** → Убедиться что:
   - Ключи добавлены в оба файла локализации
   - Одинаковая структура в ru.json и en.json
   - Валидность JSON файлов
   - Отсутствие fallback текста в t() функциях
   - Отсутствие хардкода русского текста в компонентах
10. **Обновление документации** → Финальная актуализация после изменений
11. **Ревью** → Код, диаграммы и документация соответствуют стандартам
12. **Деплой** → Только при соблюдении всех требований
